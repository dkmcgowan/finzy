Microsoft Store support: Finzy is now available as an MSIX package for Windows (x64 and arm64). Bug fixes and improvements across all platforms.

Full release notes

## Microsoft Store

- **MSIX packages** for Windows x64 and arm64, available from the Microsoft Store
- **Store listing assets**: logos (300×300, 150×150, 71×71) for Partner Center
- MSIX build fixes for arm64 architecture
- Partner Center identity and display name aligned for Store submission

## Build & CI

- MSIX always built on Windows package runs; publisher from `MSIX_PUBLISHER` secret
- MSIX output path fixed for attestation and artifact upload
- Generate-icons workflow includes Microsoft Store logos

## Other

- Bug fixes and improvements

